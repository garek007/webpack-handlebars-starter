     <!DOCTYPE html>
     <html>
     <head>
         <title>{{htmlWebpackplugin.options.title}}</title>
     </head>
     <body>
        {{myVariable}}
         <h1>Hello, {{name}}!</h1>
         {{> input}}
         <ul>
             {{#each items}}
                 <li>{{this}}</li>
             {{/each}}
         </ul>
         {{test}}
     </body>
     </html>